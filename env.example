# SMTP Configuration for Nodemailer
# You can use Gmail, SendGrid, AWS SES, or any SMTP service

# SMTP Host (e.g., smtp.gmail.com for Gmail, smtp.sendgrid.net for SendGrid)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Use SSL/TLS (true for port 465, false for port 587)
SMTP_SECURE=false

# SMTP Username (your email address or API username)
SMTP_USER=your-email@gmail.com

# SMTP Password (your email password or API key)
# For Gmail: Use an App Password (https://support.google.com/accounts/answer/185833)
SMTP_PASS=your-app-password

# From Email Address (the email that will appear as sender)
SMTP_FROM=your-email@gmail.com

# Admin Email (where contact form submissions will be sent)
ADMIN_EMAIL=your-email@gmail.com

# Example configurations for different providers:

# Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-character-app-password
# SMTP_FROM=your-email@gmail.com
# ADMIN_EMAIL=your-email@gmail.com

# SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=your-verified-sender@yourdomain.com
# ADMIN_EMAIL=your-email@gmail.com

# AWS SES:
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ses-smtp-username
# SMTP_PASS=your-ses-smtp-password
# SMTP_FROM=your-verified-email@yourdomain.com
# ADMIN_EMAIL=your-email@gmail.com
